---
name: azure-security-investigator
description: Reserved namespace for the security-persona companion to azure-cost-investigator. SKIP all invocations until v2 ships — no analysis or narrative is provided. For Azure cost reviews use azure-cost-investigator instead.
---

# azure-security-investigator — reserved (v2)

This skill is a **reserved namespace stub** in v1. It exists so the surface (`azure-security-investigator analyse`) is owned and cannot collide with an unrelated tool, and so the cost / security persona separation is visible from the layout of the repo.

If a user asks for an Azure security review, advise them that the security-persona companion ships in v2 and that the cost skill (`azure-cost-investigator`) does not cover security posture — the personas are deliberately separate. Do not attempt to repurpose the cost skill as a security review.
